# Environment configuration for Twiddle Docker services

# Temporal settings
TEMPORAL_VERSION=1.24.2

# Airflow settings
AIRFLOW_VERSION=2.8.1
AIRFLOW_ADMIN_USER=airflow
AIRFLOW_ADMIN_PASSWORD=airflow

# Set this on Linux to the output of: id -u
# On Mac/Windows, you can leave it as 50000
AIRFLOW_UID=50000

# Additional pip packages (optional)
# _PIP_ADDITIONAL_REQUIREMENTS=apache-airflow-providers-http
